diff --git a/lib/index.js b/lib/index.js
index 75fae74961306a992cde304e7311812c6e712de8..13d75bf8420abbbbd30ef365b3f08ea77b86f4a8 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -553,7 +553,8 @@ function spawnSubprocess(spec, internals = {}) {
 			outMode === "inherit" ? "inherit" : "pipe",
 			errMode === "inherit" ? "inherit" : "pipe"
 		],
-		detached: platform !== "win32"
+		detached: platform !== "win32",
+		windowsHide: platform === "win32"
 	});
 	const collectStream = (mode, stream, label) => {
 		if (!isCollect(mode) || stream === null) return void 0;
