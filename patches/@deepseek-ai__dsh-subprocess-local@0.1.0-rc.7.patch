diff --git a/lib/index.js b/lib/index.js
index 75fae74961306a992cde304e7311812c6e712de8..22da8e4ae6def4465f0f3a23039e0c0f366c5639 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -497,7 +497,10 @@ function taskkillProcessTree(pid) {
 		String(pid),
 		"/T",
 		"/F"
-	], { stdio: "ignore" });
+	], {
+		stdio: "ignore",
+		windowsHide: true
+	});
 }
 /**
 * Signal a detached process tree with platform-correct semantics: POSIX
@@ -553,7 +556,8 @@ function spawnSubprocess(spec, internals = {}) {
 			outMode === "inherit" ? "inherit" : "pipe",
 			errMode === "inherit" ? "inherit" : "pipe"
 		],
-		detached: platform !== "win32"
+		detached: platform !== "win32",
+		windowsHide: platform === "win32"
 	});
 	const collectStream = (mode, stream, label) => {
 		if (!isCollect(mode) || stream === null) return void 0;
