diff --git a/lib/hot.js b/lib/hot.js
index b122774c58dbaf76d98dbcce75cabd995cac7b87..1a0f59f89d6e4596bd80d0a8b5d77f0702f86f99 100644
--- a/lib/hot.js
+++ b/lib/hot.js
@@ -160,6 +160,10 @@ function uniqueStrings(value) {
     }
     return out;
 }
+/** Read only the old theme-specific state; generic `disabled` is never legacy skin data. */
+function legacyDisabledSkinsOf(state) {
+    return state.disabledSkins === undefined ? undefined : new Set(uniqueStrings(state.disabledSkins));
+}
 /**
  * Read the whole market state. Legacy `disabledSkins` (the pre-#60
  * theme-only key) still loads; every new write uses the generic `disabled`
@@ -168,6 +172,7 @@ function uniqueStrings(value) {
 export function readMarketState(profileDir) {
     try {
         const state = JSON.parse(readFileSync(stateFile(profileDir), 'utf8'));
+        const legacyDisabledSkins = legacyDisabledSkinsOf(state);
         const disabled = uniqueStrings(state.disabled !== undefined ? state.disabled : state.disabledSkins);
         const groups = {};
         if (state.groups !== null && typeof state.groups === 'object' && !Array.isArray(state.groups)) {
@@ -180,13 +185,14 @@ export function readMarketState(profileDir) {
             groups,
             groupOrder: uniqueStrings(state.groupOrder),
             channel: asChannel(state.channel) ?? undefined,
+            ...(legacyDisabledSkins === undefined ? {} : { legacyDisabledSkins }),
         };
     }
     catch {
         return { disabled: new Set(), groups: {}, groupOrder: [] };
     }
 }
-/** Persist the whole market state; `disabled` is the single written key. */
+/** Persist the whole market state, retaining only pending Desktop legacy migration names. */
 export function writeMarketState(profileDir, state) {
     mkdirSync(join(profileDir, HOT_DIR), { recursive: true, mode: 0o700 });
     writeFileSync(stateFile(profileDir), JSON.stringify({
@@ -196,6 +202,11 @@ export function writeMarketState(profileDir, state) {
         // Omitted while unchosen, so "never picked" survives a round trip and
         // keeps deriving from the running build.
         ...(state.channel === undefined ? {} : { channel: state.channel }),
+        // A legacy key is retained only when Desktop could not yet map it to a
+        // loader row. Do not drop groups/order/channel while that retry waits.
+        ...(state.legacyDisabledSkins === undefined || state.legacyDisabledSkins.size === 0
+            ? {}
+            : { disabledSkins: [...state.legacyDisabledSkins] }),
     }));
 }
 /** Plugins the user switched off; skipped by the boot re-mount. */
diff --git a/lib/index.js b/lib/index.js
index 9da4272af90ac48a3b018bb8d9b8ab7d0fd2e76c..7612194ced037f103c063302c6164548682f4154 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -58,12 +58,17 @@ export function apply(ctx, config) {
             const current = desktopProfiles.current;
             const service = desktopCtx.desktopPnpm;
             const runtime = createDesktopPluginRuntime(service, current.dir);
+            const skinState = desktopCtx.get('desktopSkinState');
+            if (skinState === undefined) {
+                host.logger?.warn('[dsh-market] desktopSkinState unavailable; using legacy theme persistence');
+            }
             const resolved = {
                 profile: current.name,
                 profileDirectory: current.dir,
                 // Relaunching a raw Electron process would bypass Desktop's launcher
                 // lifecycle. The shell remains responsible for restart in this mode.
                 allowRestart: false,
+                skinState,
             };
             const desktopHost = desktopCtx;
             desktopHost.effect(() => {
diff --git a/lib/profile.js b/lib/profile.js
index 178b7f44b222b9a2c1f1b189465edea4e3b1f791..df87e6ced517d8194d3105c88663d9eff4c247ab 100644
--- a/lib/profile.js
+++ b/lib/profile.js
@@ -30,13 +30,27 @@ export const INBOX_BUNDLES = new Set([
     '@deepseek-ai/dsh-web-app',
     '@deepseek-ai/dsh-headless',
 ]);
-/** Community dependencies of the profile (in-box bundles filtered out). */
+/** Packages owned by Desktop's profile reconciler rather than the market. */
+function desktopManagedNames(directory) {
+    try {
+        const records = JSON.parse(readFileSync(join(directory, '.dsh-desktop-links.json'), 'utf8'));
+        if (records === null || typeof records !== 'object' || Array.isArray(records))
+            return new Set();
+        return new Set(Object.keys(records));
+    }
+    catch {
+        return new Set();
+    }
+}
+/** Community dependencies of the profile (in-box and Desktop-managed packages filtered out). */
 export function readInstalled(profile, explicitDir) {
     try {
-        const manifest = JSON.parse(readFileSync(join(profileDir(profile, explicitDir), 'package.json'), 'utf8'));
+        const directory = profileDir(profile, explicitDir);
+        const manifest = JSON.parse(readFileSync(join(directory, 'package.json'), 'utf8'));
+        const desktopManaged = desktopManagedNames(directory);
         const installed = {};
         for (const [name, spec] of Object.entries(manifest.dependencies ?? {})) {
-            if (!INBOX_BUNDLES.has(name))
+            if (!INBOX_BUNDLES.has(name) && !desktopManaged.has(name))
                 installed[name] = spec;
         }
         return installed;
@@ -305,6 +319,9 @@ export function entryArtifactExists(dir) {
                 if (typeof value === 'string')
                     candidates.push(value);
         }
+        const bundlePatch = manifest.dsh?.bundle?.patch;
+        if (candidates.length === 0 && typeof bundlePatch === 'string')
+            candidates.push(bundlePatch);
         if (candidates.length === 0)
             candidates.push('index.js');
         return candidates.some(rel => existsSync(join(dir, rel)));
diff --git a/lib/routes.js b/lib/routes.js
index 6e1dae8025d47205473bc4d1c36ee93213c84c88..0c501c9093933a93cb24d8ec7f007f0abeff6dc8 100644
--- a/lib/routes.js
+++ b/lib/routes.js
@@ -130,19 +130,63 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
     // Boot-time wipe: stale hot-mount inputs from a previous session must never
     // survive into a composition where the bundle layer already covers them.
     cleanHotDir(activeProfileDir);
-    // The user's persisted choices: the generic disable list (legacy
-    // disabledSkins loads transparently) plus custom groups. Every toggle,
-    // group, install and uninstall mutates this shared state and persists it.
+    // The user's persisted choices: the generic disable list plus custom
+    // groups. `legacyDisabledSkins` is deliberately distinct: it is only the
+    // pre-#60 theme key and must never be inferred from generic disables.
     const marketState = readMarketState(activeProfileDir);
     const disabled = marketState.disabled;
     const groups = marketState.groups;
     const groupOrder = marketState.groupOrder;
+    const legacyDisabledSkins = marketState.legacyDisabledSkins;
+    function persistMarketState() {
+        writeMarketState(activeProfileDir, marketState);
+    }
+    function migrateLegacySkinNames(names) {
+        const skinState = config.skinState;
+        if (skinState === undefined || legacyDisabledSkins === undefined)
+            return new Set();
+        const candidates = [...new Set(names)].filter(name => legacyDisabledSkins.has(name));
+        if (candidates.length === 0)
+            return new Set();
+        try {
+            const migrated = skinState.migrateLegacy(candidates, host.loader.entries());
+            const applied = new Set();
+            for (const name of migrated) {
+                if (!legacyDisabledSkins.delete(name))
+                    continue;
+                disabled.delete(name);
+                applied.add(name);
+            }
+            if (applied.size > 0) {
+                if (legacyDisabledSkins.size === 0)
+                    marketState.legacyDisabledSkins = undefined;
+                persistMarketState();
+            }
+            return applied;
+        }
+        catch (error) {
+            const message = error instanceof Error ? error.message : String(error);
+            host.logger?.warn(`[dsh-market] legacy desktop skin migration failed: ${message}`);
+            logEvent('warn', 'boot', `legacy skin migration failed: ${message}`);
+            return new Set();
+        }
+    }
     // A choice made in a previous session outranks whatever the entry layer
     // composed, which is only ever a default.
     if (marketState.channel !== undefined)
         config.channel = marketState.channel;
     const activeChannel = () => resolveChannel(config.channel, marketVersion());
-    const themes = createThemeManager(host, config.profile, disabled, activeProfileDir);
+    if (config.skinState !== undefined && legacyDisabledSkins !== undefined && legacyDisabledSkins.size > 0) {
+        migrateLegacySkinNames(legacyDisabledSkins);
+        if (legacyDisabledSkins.size > 0) {
+            logEvent('warn', 'boot', `legacy skin disables pending migration: ${[...legacyDisabledSkins].join(', ')}`);
+        }
+    }
+    // The generic list continues to own all ordinary plugin switches. Desktop
+    // skin choices instead live in its managed profile patch, so never feed a
+    // generic disabled name into the skin persistence channel by accident.
+    const themeDisabled = config.skinState === undefined ? disabled : new Set();
+    const themes = createThemeManager(host, config.profile, themeDisabled, activeProfileDir, config.skinState);
     // Client-only packages (dsh.client without dsh.bundle) are invisible to the
     // bundle layer in every boot; the market shim-mounts them so their client
     // bundles are actually served.
@@ -160,14 +204,32 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
     });
     // Self-healing guard: dsh's own patch overlay can re-update entries during
     // activation and wipe the runtime disabled flag — whenever a fiber comes
-    // up for a plugin the user switched off, put it back down.
+    // up for a plugin the user switched off, put it back down. This applies to
+    // every generic plugin disable; a legacy Desktop skin retries migration
+    // once its loader row finally exists.
     host.on?.('internal/plugin', (fiber) => {
         const name = fiber.entry?.options?.name;
-        if (name !== undefined && disabled.has(name))
+        if (name === undefined)
+            return;
+        if (disabled.has(name))
             void themes.setEntryDisabled(name, true);
+        if (config.skinState !== undefined && legacyDisabledSkins?.has(name)) {
+            const migrated = migrateLegacySkinNames([name]);
+            if (migrated.has(name))
+                logEvent('info', 'boot', `legacy skin disable migrated: ${name}`);
+        }
     });
     let installing = false;
     let restarting = false;
+    function rejectDesktopPluginMutation(response) {
+        if (config.profileDirectory === undefined)
+            return false;
+        sendJson(response, 409, {
+            code: 'desktop-plugin-manager-required',
+            error: '桌面版的插件安装、更新、卸载和恢复统一由“工具 -> 扩展坞”管理，以便启动失败时自动回滚。 / Desktop plugin installs, updates, removals, and restores must use Tools -> Extension Dock so failed startups can be rolled back.',
+        });
+        return true;
+    }
     // UI-state flags ONLY: mutual exclusion is enforced by withMutationLock
     // below (one promise chain every mutating route appends to), never by
     // these booleans — a promise-chain serialization cannot be raced by
@@ -259,7 +321,7 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                 ok = true;
             }
         }
-        writeMarketState(dir, { disabled, groups, groupOrder });
+        persistMarketState();
         return { ok, reason };
     }
     /**
@@ -369,7 +431,7 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
         removeRowBlocks(userPatchPath, rowIdsForPackage(host, activeProfileDir, name));
         disabled.delete(name);
         removeFromGroups({ groups, groupOrder }, name);
-        writeMarketState(activeProfileDir, { disabled, groups, groupOrder });
+        persistMarketState();
         return { ok: true, hot, detail: null };
     }
     async function restoreBackup(value) {
@@ -492,6 +554,8 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                     response.end();
                     return;
                 }
+                if (rejectDesktopPluginMutation(response))
+                    return;
                 if (!sameOrigin(request))
                     return sendJson(response, 403, { error: 'untrusted origin' });
                 try {
@@ -1008,7 +1072,7 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                         error = result.error;
                     }
                     if (ok)
-                        writeMarketState(activeProfileDir, { disabled, groups, groupOrder });
+                        persistMarketState();
                     logEvent(ok ? 'info' : 'warn', 'groups', `${action}${typeof body.name === 'string' ? ' ' + body.name : ''}${ok ? '' : ` — ${error ?? ''}`}`);
                     sendJson(response, ok ? 200 : 400, {
                         ok,
@@ -1126,6 +1190,8 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                     response.end();
                     return;
                 }
+                if (rejectDesktopPluginMutation(response))
+                    return;
                 if (!sameOrigin(request)) {
                     sendJson(response, 403, { error: 'untrusted origin' });
                     return;
@@ -1427,7 +1493,7 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                     // survives a restart — a setting that forgets is a setting the
                     // user has to make again every boot.
                     marketState.channel = wanted;
-                    writeMarketState(activeProfileDir, marketState);
+                    persistMarketState();
                     // The cached listing was computed for the old channel, so the very
                     // next check would answer for a setting that no longer applies.
                     invalidateUpdates();
@@ -1448,6 +1514,8 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                     response.end();
                     return;
                 }
+                if (rejectDesktopPluginMutation(response))
+                    return;
                 // The same single door the restart route uses — and only it. Both
                 // end the market's life in this process, so neither may be driven by
                 // a remote or forwarded client. A separate `sameOrigin` call would
@@ -1703,6 +1771,8 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                     response.end();
                     return;
                 }
+                if (rejectDesktopPluginMutation(response))
+                    return;
                 if (!sameOrigin(request)) {
                     sendJson(response, 403, { error: 'untrusted origin' });
                     return;
@@ -1761,7 +1831,7 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                             // rule so no group toggle ever targets a ghost member.
                             disabled.delete(name);
                             removeFromGroups({ groups, groupOrder }, name);
-                            writeMarketState(activeProfileDir, { disabled, groups, groupOrder });
+                            persistMarketState();
                         }
                         logEvent(ok || cancelled ? 'info' : 'error', 'uninstall', `${name} exit=${String(result.exitCode)}${cancelled ? ' CANCELLED' : ''}${ok ? ` live-removed=${String(hot)}` : cancelled ? '' : ` stderr=${result.stderr.slice(-300)}`}`);
                         sendJson(response, ok || cancelled ? 200 : result.busy === true ? 409 : 502, {
@@ -1797,6 +1867,8 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                     response.end();
                     return;
                 }
+                if (rejectDesktopPluginMutation(response))
+                    return;
                 if (!sameOrigin(request)) {
                     sendJson(response, 403, { error: 'untrusted origin' });
                     return;
@@ -1866,6 +1938,8 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                     response.end();
                     return;
                 }
+                if (rejectDesktopPluginMutation(response))
+                    return;
                 if (!sameOrigin(request)) {
                     sendJson(response, 403, { error: 'untrusted origin' });
                     return;
@@ -2020,7 +2094,7 @@ export function mountMarketRoutes(host, config, commandRuntime, agentsLookup) {
                                 // running) and persist before the activation loop.
                                 for (const name of added)
                                     disabled.delete(name);
-                                writeMarketState(activeProfileDir, { disabled, groups, groupOrder });
+                                persistMarketState();
                                 // Theme installs auto-activate (and deactivate the previous
                                 // theme) so the result is visible right after the refresh.
                                 hot = true;
diff --git a/lib/themes.js b/lib/themes.js
index 44a0cc84ac2a13383397ee74fcf765f477c26363..bf236d1a6e44cab1942306061431ee27bdb50e15 100644
--- a/lib/themes.js
+++ b/lib/themes.js
@@ -14,7 +14,7 @@ import { repoOf } from './sources.js';
  * themes the user switched off — the caller owns reading it at boot and
  * replaying it; the manager mutates and persists it on switches.
  */
-export function createThemeManager(host, profile, disabledThemes, explicitDir) {
+export function createThemeManager(host, profile, disabledThemes, explicitDir, persistence) {
     const activeProfileDir = profileDir(profile, explicitDir);
     /** Installed package names classified as themes by the registry's theme category. */
     async function installedThemeNames() {
@@ -75,28 +75,43 @@ export function createThemeManager(host, profile, disabledThemes, explicitDir) {
     /**
      * Make `name` the one active theme: deactivate every other installed theme
      * (market hot mounts unmount; bundle-layer entries live-disable) and bring
-     * it up. The choice persists in state.json and is replayed at boot.
+     * it up. Desktop persists into the shared home patch; ordinary DSH retains
+     * state.json for backward compatibility.
      */
     async function activateTheme(name) {
         const themes = await installedThemeNames();
+        // Desktop's home patch is the durable source of truth. Commit it before
+        // touching live entries so a rejected persistence request cannot leave a
+        // theme active only in memory.
+        if (persistence !== undefined) {
+            persistence.activateBundleTheme(name, themes, host.loader.entries());
+            disabledThemes.clear();
+            for (const disabled of persistence.disabledNames(themes, host.loader.entries()))
+                disabledThemes.add(disabled);
+        }
         for (const other of themes) {
             if (other === name)
                 continue;
             if (listHotMounts().includes(other)) {
                 await hotUnmount(other);
-                disabledThemes.add(other);
+                if (persistence === undefined)
+                    disabledThemes.add(other);
             }
             else if (await setEntryDisabled(other, true)) {
-                disabledThemes.add(other);
+                if (persistence === undefined)
+                    disabledThemes.add(other);
             }
         }
-        disabledThemes.delete(name);
-        writeDisabled(activeProfileDir, disabledThemes);
-        if (listHotMounts().includes(name))
-            return true;
-        if (await setEntryDisabled(name, false))
-            return true;
-        return (await hotMount(host, activeProfileDir, name)).ok;
+        const activated = listHotMounts().includes(name)
+            || await setEntryDisabled(name, false)
+            || (await hotMount(host, activeProfileDir, name)).ok;
+        if (!activated)
+            return false;
+        if (persistence === undefined) {
+            disabledThemes.delete(name);
+            writeDisabled(activeProfileDir, disabledThemes);
+        }
+        return true;
     }
     return { installedThemeNames, setEntryDisabled, activateTheme };
 }
diff --git a/lib/types/hot.d.ts b/lib/types/hot.d.ts
index 37ae1fe6d55e21bd7bc3c778f071ee63bc068585..0ac8cb31e644c8c0e42b37bafa4c6e2da18c6a14 100644
--- a/lib/types/hot.d.ts
+++ b/lib/types/hot.d.ts
@@ -58,16 +58,22 @@ export interface MarketState {
      * puts you on the beta channel without a second step. Once chosen, the
      * choice is the answer — including "stable" while a beta is running, which
      * is how someone gets back off the channel.
-     */
+    */
     channel?: Channel;
+    /**
+     * Theme-only state written by dshmarket before generic plugin disables
+     * existed. Desktop retains this key only while desktopSkinState is waiting
+     * to migrate a name whose loader row has not appeared yet.
+     */
+    legacyDisabledSkins?: Set<string>;
 }
 /**
  * Read the whole market state. Legacy `disabledSkins` (the pre-#60
- * theme-only key) still loads; every new write uses the generic `disabled`
- * key (#60).
+ * theme-only key) still loads; pending Desktop migration names remain
+ * distinguishable from generic `disabled` entries.
  */
 export declare function readMarketState(profileDir: string): MarketState;
-/** Persist the whole market state; `disabled` is the single written key. */
+/** Persist the whole market state, retaining only pending Desktop legacy migration names. */
 export declare function writeMarketState(profileDir: string, state: MarketState): void;
 /** Plugins the user switched off; skipped by the boot re-mount. */
 export declare function readDisabled(profileDir: string): Set<string>;
diff --git a/lib/types/profile.d.ts b/lib/types/profile.d.ts
index 6f96b4fa06db12fa366af1f068f53e74070025e4..b6624c4c008f346aac835dda2db49f72908ec4c1 100644
--- a/lib/types/profile.d.ts
+++ b/lib/types/profile.d.ts
@@ -17,7 +17,7 @@ export declare function profileDir(profile: string, explicitDir?: string): strin
  * (Diagnosis and fix proposed in #28 by @Lograthmic.)
  */
 export declare const INBOX_BUNDLES: Set<string>;
-/** Community dependencies of the profile (in-box bundles filtered out). */
+/** Community dependencies of the profile (in-box and Desktop-managed packages filtered out). */
 export declare function readInstalled(profile: string, explicitDir?: string): Record<string, string>;
 /**
  * RAW dependency map of the profile manifest — including the in-box bundles
diff --git a/lib/types/routes.d.ts b/lib/types/routes.d.ts
index 621212206d58c54967592c89dcd7414d710ce462..b340a14d4df89c1af75bb63e4a925e34ad2104a4 100644
--- a/lib/types/routes.d.ts
+++ b/lib/types/routes.d.ts
@@ -11,7 +11,7 @@ import type { IncomingMessage, ServerResponse } from 'node:http';
 import { type PluginCommandRuntime } from './dsh-cli.ts';
 import { type AgentsLookup } from './agents.ts';
 import { type Channel } from './channels.ts';
-import { type LoaderEntry } from './themes.ts';
+import { type LoaderEntry, type ThemePersistence } from './themes.ts';
 export type { LoaderEntry } from './themes.ts';
 export type { UpdateStatus } from './updates.ts';
 export interface WebServerService {
@@ -51,6 +51,10 @@ export interface MarketConfig {
     allowRestart?: boolean;
     /** Which release channel the market offers ITSELF from; other plugins never follow it. */
     channel?: Channel;
+    /** Desktop's shared home-patch channel; absent in ordinary DSH. */
+    skinState?: ThemePersistence & {
+        migrateLegacy(disabledNames: Iterable<string>, entries: Iterable<LoaderEntry>): Set<string>;
+    };
 }
 export declare function marketVersion(): string;
 /**
diff --git a/lib/types/themes.d.ts b/lib/types/themes.d.ts
index 5b152d786883c0967a4ac976a6964c0ce02f81a7..fcffa8c673edd41b64a7d125049700f7bc564dca 100644
--- a/lib/types/themes.d.ts
+++ b/lib/types/themes.d.ts
@@ -26,6 +26,11 @@ export interface ThemeHost {
         dispose(): Promise<unknown> | void;
     };
 }
+/** Desktop-owned durable skin state. Ordinary DSH keeps using state.json. */
+export interface ThemePersistence {
+    disabledNames(themeNames: Iterable<string>, entries: Iterable<LoaderEntry>): Set<string>;
+    activateBundleTheme(name: string, themeNames: Iterable<string>, entries: Iterable<LoaderEntry>): void;
+}
 /** Manages theme exclusivity for one profile. */
 export interface ThemeManager {
     installedThemeNames(): Promise<Set<string>>;
@@ -37,4 +42,4 @@ export interface ThemeManager {
  * themes the user switched off — the caller owns reading it at boot and
  * replaying it; the manager mutates and persists it on switches.
  */
-export declare function createThemeManager(host: ThemeHost, profile: string, disabledThemes: Set<string>, explicitDir?: string): ThemeManager;
+export declare function createThemeManager(host: ThemeHost, profile: string, disabledThemes: Set<string>, explicitDir?: string, persistence?: ThemePersistence): ThemeManager;
diff --git a/lib/verify.js b/lib/verify.js
index 4ae021ab5e86df9d00df8f9362f3e2816b1b36f8..c773a8a688219d73222dd9ae939405600d5d1664 100644
--- a/lib/verify.js
+++ b/lib/verify.js
@@ -195,6 +195,16 @@ export function verifyActivation(profile, name, live = new Set(listHotMounts()),
             hot: false,
         };
     }
+    if (dsh.bundle !== undefined) {
+        return {
+            state: 'inert',
+            reasons: [
+                '已声明 dsh.bundle,但当前未进入 profile bundle 层;请通过宿主插件管理器启用 / dsh.bundle is declared but the package is not in the profile bundle layer; enable it through the host plugin manager',
+            ],
+            bundle: false,
+            hot: false,
+        };
+    }
     // Not a profile-layer plugin. Client-only packages never enter bundles
     // (the dsh CLI skips them), so the market shim-mounts them at boot —
     // they still work, but "installed" never means "bundle layer".
diff --git a/src/hot.ts b/src/hot.ts
index 688295171a521e699f65aa7c7a7c67b009396db9..c10043564ad23dd159e61dfa49ddb4d1ed020649 100644
--- a/src/hot.ts
+++ b/src/hot.ts
@@ -178,6 +178,12 @@ export interface MarketState {
    * is how someone gets back off the channel.
    */
   channel?: Channel
+  /**
+   * Theme-only state written by dshmarket before generic plugin disables
+   * existed. Desktop retains this key only while desktopSkinState is waiting
+   * to migrate a name whose loader row has not appeared yet.
+   */
+  legacyDisabledSkins?: Set<string>
 }
 
 /** Unique non-empty strings in `value`, order preserved. */
@@ -193,6 +199,11 @@ function uniqueStrings(value: unknown): string[] {
   return out
 }
 
+/** Read only the old theme-specific state; generic `disabled` is never legacy skin data. */
+function legacyDisabledSkinsOf(state: { disabledSkins?: unknown }): Set<string> | undefined {
+  return state.disabledSkins === undefined ? undefined : new Set(uniqueStrings(state.disabledSkins))
+}
+
 /**
  * Read the whole market state. Legacy `disabledSkins` (the pre-#60
  * theme-only key) still loads; every new write uses the generic `disabled`
@@ -207,6 +218,7 @@ export function readMarketState(profileDir: string): MarketState {
       groupOrder?: unknown
       channel?: unknown
     }
+    const legacyDisabledSkins = legacyDisabledSkinsOf(state)
     const disabled = uniqueStrings(state.disabled !== undefined ? state.disabled : state.disabledSkins)
     const groups: Record<string, string[]> = {}
     if (state.groups !== null && typeof state.groups === 'object' && !Array.isArray(state.groups)) {
@@ -219,13 +231,14 @@ export function readMarketState(profileDir: string): MarketState {
       groups,
       groupOrder: uniqueStrings(state.groupOrder),
       channel: asChannel(state.channel) ?? undefined,
+      ...(legacyDisabledSkins === undefined ? {} : { legacyDisabledSkins }),
     }
   } catch {
     return { disabled: new Set(), groups: {}, groupOrder: [] }
   }
 }
 
-/** Persist the whole market state; `disabled` is the single written key. */
+/** Persist the whole market state, retaining only pending Desktop legacy migration names. */
 export function writeMarketState(profileDir: string, state: MarketState): void {
   mkdirSync(join(profileDir, HOT_DIR), { recursive: true, mode: 0o700 })
   writeFileSync(stateFile(profileDir), JSON.stringify({
@@ -235,6 +248,11 @@ export function writeMarketState(profileDir: string, state: MarketState): void {
     // Omitted while unchosen, so "never picked" survives a round trip and
     // keeps deriving from the running build.
     ...(state.channel === undefined ? {} : { channel: state.channel }),
+    // A legacy key is retained only when Desktop could not yet map it to a
+    // loader row. Do not drop groups/order/channel while that retry waits.
+    ...(state.legacyDisabledSkins === undefined || state.legacyDisabledSkins.size === 0
+      ? {}
+      : { disabledSkins: [...state.legacyDisabledSkins] }),
   }))
 }
 
diff --git a/src/index.ts b/src/index.ts
index e06cdda27dcd0416548c8ed856836b812eefc8d0..9412dc24466fb547e9cfbac0a0a1e93054e0c45a 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -83,12 +83,17 @@ export function apply(ctx: Context, config?: Config): void {
       const current = desktopProfiles.current
       const service = (desktopCtx as unknown as { desktopPnpm: DesktopPnpmLike }).desktopPnpm
       const runtime = createDesktopPluginRuntime(service, current.dir)
+      const skinState = desktopCtx.get('desktopSkinState') as MarketConfig['skinState']
+      if (skinState === undefined) {
+        host.logger?.warn('[dsh-market] desktopSkinState unavailable; using legacy theme persistence')
+      }
       const resolved: MarketConfig = {
         profile: current.name,
         profileDirectory: current.dir,
         // Relaunching a raw Electron process would bypass Desktop's launcher
         // lifecycle. The shell remains responsible for restart in this mode.
         allowRestart: false,
+        skinState,
       }
       const desktopHost = desktopCtx as unknown as MarketEffectHost
       desktopHost.effect(() => {
diff --git a/src/profile.ts b/src/profile.ts
index 2a4b43874bd73f55e423ae25d53a19682ada943e..22bcd4d3ead9c4fd5e88be51c783c0a06987f9c5 100644
--- a/src/profile.ts
+++ b/src/profile.ts
@@ -33,15 +33,28 @@ export const INBOX_BUNDLES = new Set([
   '@deepseek-ai/dsh-headless',
 ])
 
-/** Community dependencies of the profile (in-box bundles filtered out). */
+/** Packages owned by Desktop's profile reconciler rather than the market. */
+function desktopManagedNames(directory: string): Set<string> {
+  try {
+    const records = JSON.parse(readFileSync(join(directory, '.dsh-desktop-links.json'), 'utf8')) as unknown
+    if (records === null || typeof records !== 'object' || Array.isArray(records)) return new Set()
+    return new Set(Object.keys(records))
+  } catch {
+    return new Set()
+  }
+}
+
+/** Community dependencies of the profile (in-box and Desktop-managed packages filtered out). */
 export function readInstalled(profile: string, explicitDir?: string): Record<string, string> {
   try {
-    const manifest = JSON.parse(readFileSync(join(profileDir(profile, explicitDir), 'package.json'), 'utf8')) as {
+    const directory = profileDir(profile, explicitDir)
+    const manifest = JSON.parse(readFileSync(join(directory, 'package.json'), 'utf8')) as {
       dependencies?: Record<string, string>
     }
+    const desktopManaged = desktopManagedNames(directory)
     const installed: Record<string, string> = {}
     for (const [name, spec] of Object.entries(manifest.dependencies ?? {})) {
-      if (!INBOX_BUNDLES.has(name)) installed[name] = spec
+      if (!INBOX_BUNDLES.has(name) && !desktopManaged.has(name)) installed[name] = spec
     }
     return installed
   } catch {
@@ -310,6 +323,7 @@ export function entryArtifactExists(dir: string): boolean {
     const manifest = JSON.parse(readFileSync(join(dir, 'package.json'), 'utf8')) as {
       main?: string
       exports?: Record<string, unknown> | string
+      dsh?: { bundle?: { patch?: unknown } }
     }
     const candidates: string[] = []
     if (typeof manifest.main === 'string') candidates.push(manifest.main)
@@ -320,6 +334,8 @@ export function entryArtifactExists(dir: string): boolean {
     else if (rootExport !== null && typeof rootExport === 'object') {
       for (const value of Object.values(rootExport)) if (typeof value === 'string') candidates.push(value)
     }
+    const bundlePatch = manifest.dsh?.bundle?.patch
+    if (candidates.length === 0 && typeof bundlePatch === 'string') candidates.push(bundlePatch)
     if (candidates.length === 0) candidates.push('index.js')
     return candidates.some(rel => existsSync(join(dir, rel)))
   } catch {
diff --git a/src/routes.ts b/src/routes.ts
index a094b23bd0ae8b9bc589ce7d417d469ed949b63d..99239e03f612b43648c16afee8e68c20ef04671c 100644
--- a/src/routes.ts
+++ b/src/routes.ts
@@ -33,7 +33,7 @@ import { findInstalledAlias, gitAllowBuildsKey, installTargetFor } from './sourc
 import { isStaleUpdate, parseIgnoredBuilds, parsePrepareNotAllowed, RELEASE_AGE_OVERRIDE, retargetCollections, validateAddedPlugins, withHoistRecovery } from './install.ts'
 import { asChannel, CHANNELS, DIST_TAG, resolveChannel, type Channel } from './channels.ts'
 import { checkUpdates, fetchNpmLatest, invalidateUpdates, isUpgrade, latestPublishedRecently, versionOnChannel } from './updates.ts'
-import { createThemeManager, type LoaderEntry } from './themes.ts'
+import { createThemeManager, type LoaderEntry, type ThemePersistence } from './themes.ts'
 import { readJsonBody, sameOrigin, sendJson } from './http.ts'
 import { restartAllowed, scheduleRestart, servingPort, trustedRestartRequest, trustedDownloadRequest } from './restart.ts'
 import { activationAfterReplace, hasHostHalf, verifyActivation } from './verify.ts'
@@ -77,6 +77,10 @@ export interface MarketConfig {
   allowRestart?: boolean
   /** Which release channel the market offers ITSELF from; other plugins never follow it. */
   channel?: Channel
+  /** Desktop's shared home-patch channel; absent in ordinary DSH. */
+  skinState?: ThemePersistence & {
+    migrateLegacy(disabledNames: Iterable<string>, entries: Iterable<LoaderEntry>): Set<string>
+  }
 }
 
 const PROFILE_RE = /^[A-Za-z0-9_-]+$/
@@ -182,18 +186,57 @@ export function mountMarketRoutes(
   // Boot-time wipe: stale hot-mount inputs from a previous session must never
   // survive into a composition where the bundle layer already covers them.
   cleanHotDir(activeProfileDir)
-  // The user's persisted choices: the generic disable list (legacy
-  // disabledSkins loads transparently) plus custom groups. Every toggle,
-  // group, install and uninstall mutates this shared state and persists it.
+  // The user's persisted choices: the generic disable list plus custom
+  // groups. `legacyDisabledSkins` is deliberately distinct: it is only the
+  // pre-#60 theme key and must never be inferred from generic disables.
   const marketState = readMarketState(activeProfileDir)
   const disabled = marketState.disabled
   const groups = marketState.groups
   const groupOrder = marketState.groupOrder
+  const legacyDisabledSkins = marketState.legacyDisabledSkins
+  function persistMarketState(): void {
+    writeMarketState(activeProfileDir, marketState)
+  }
+  function migrateLegacySkinNames(names: Iterable<string>): Set<string> {
+    const skinState = config.skinState
+    if (skinState === undefined || legacyDisabledSkins === undefined) return new Set()
+    const candidates = [...new Set(names)].filter(name => legacyDisabledSkins.has(name))
+    if (candidates.length === 0) return new Set()
+    try {
+      const migrated = skinState.migrateLegacy(candidates, host.loader.entries())
+      const applied = new Set<string>()
+      for (const name of migrated) {
+        if (!legacyDisabledSkins.delete(name)) continue
+        disabled.delete(name)
+        applied.add(name)
+      }
+      if (applied.size > 0) {
+        if (legacyDisabledSkins.size === 0) marketState.legacyDisabledSkins = undefined
+        persistMarketState()
+      }
+      return applied
+    } catch (error) {
+      const message = error instanceof Error ? error.message : String(error)
+      host.logger?.warn(`[dsh-market] legacy desktop skin migration failed: ${message}`)
+      logEvent('warn', 'boot', `legacy skin migration failed: ${message}`)
+      return new Set()
+    }
+  }
   // A choice made in a previous session outranks whatever the entry layer
   // composed, which is only ever a default.
   if (marketState.channel !== undefined) config.channel = marketState.channel
   const activeChannel = (): Channel => resolveChannel(config.channel, marketVersion())
-  const themes = createThemeManager(host, config.profile, disabled, activeProfileDir)
+  if (config.skinState !== undefined && legacyDisabledSkins !== undefined && legacyDisabledSkins.size > 0) {
+    migrateLegacySkinNames(legacyDisabledSkins)
+    if (legacyDisabledSkins.size > 0) {
+      logEvent('warn', 'boot', `legacy skin disables pending migration: ${[...legacyDisabledSkins].join(', ')}`)
+    }
+  }
+  // The generic list continues to own all ordinary plugin switches. Desktop
+  // skin choices instead live in its managed profile patch, so never feed a
+  // generic disabled name into the skin persistence channel by accident.
+  const themeDisabled = config.skinState === undefined ? disabled : new Set<string>()
+  const themes = createThemeManager(host, config.profile, themeDisabled, activeProfileDir, config.skinState)
 
   // Client-only packages (dsh.client without dsh.bundle) are invisible to the
   // bundle layer in every boot; the market shim-mounts them so their client
@@ -211,13 +254,28 @@ export function mountMarketRoutes(
 
   // Self-healing guard: dsh's own patch overlay can re-update entries during
   // activation and wipe the runtime disabled flag — whenever a fiber comes
-  // up for a plugin the user switched off, put it back down.
+  // up for a plugin the user switched off, put it back down. This applies to
+  // every generic plugin disable; a legacy Desktop skin retries migration
+  // once its loader row finally exists.
   host.on?.('internal/plugin', (fiber) => {
     const name = fiber.entry?.options?.name
-    if (name !== undefined && disabled.has(name)) void themes.setEntryDisabled(name, true)
+    if (name === undefined) return
+    if (disabled.has(name)) void themes.setEntryDisabled(name, true)
+    if (config.skinState !== undefined && legacyDisabledSkins?.has(name)) {
+      const migrated = migrateLegacySkinNames([name])
+      if (migrated.has(name)) logEvent('info', 'boot', `legacy skin disable migrated: ${name}`)
+    }
   })
   let installing = false
   let restarting = false
+  function rejectDesktopPluginMutation(response: ServerResponse): boolean {
+    if (config.profileDirectory === undefined) return false
+    sendJson(response, 409, {
+      code: 'desktop-plugin-manager-required',
+      error: '桌面版的插件安装、更新、卸载和恢复统一由“工具 -> 扩展坞”管理，以便启动失败时自动回滚。 / Desktop plugin installs, updates, removals, and restores must use Tools -> Extension Dock so failed startups can be rolled back.',
+    })
+    return true
+  }
   // UI-state flags ONLY: mutual exclusion is enforced by withMutationLock
   // below (one promise chain every mutating route appends to), never by
   // these booleans — a promise-chain serialization cannot be raced by
@@ -302,7 +360,7 @@ export function mountMarketRoutes(
         ok = true
       }
     }
-    writeMarketState(dir, { disabled, groups, groupOrder })
+    persistMarketState()
     return { ok, reason }
   }
 
@@ -428,7 +486,7 @@ export function mountMarketRoutes(
     removeRowBlocks(userPatchPath, rowIdsForPackage(host, activeProfileDir, name))
     disabled.delete(name)
     removeFromGroups({ groups, groupOrder }, name)
-    writeMarketState(activeProfileDir, { disabled, groups, groupOrder })
+    persistMarketState()
     return { ok: true, hot, detail: null }
   }
 
@@ -555,6 +613,7 @@ export function mountMarketRoutes(
           response.end()
           return
         }
+        if (rejectDesktopPluginMutation(response)) return
         if (!sameOrigin(request)) return sendJson(response, 403, { error: 'untrusted origin' })
         try {
           const body = await readJsonBody(request, MAX_BACKUP_BYTES + 4096) as { backup?: unknown }
@@ -1053,7 +1112,7 @@ export function mountMarketRoutes(
             ok = result.ok
             error = result.error
           }
-          if (ok) writeMarketState(activeProfileDir, { disabled, groups, groupOrder })
+          if (ok) persistMarketState()
           logEvent(ok ? 'info' : 'warn', 'groups',
             `${action}${typeof body.name === 'string' ? ' ' + body.name : ''}${ok ? '' : ` — ${error ?? ''}`}`)
           sendJson(response, ok ? 200 : 400, {
@@ -1176,6 +1235,7 @@ export function mountMarketRoutes(
           response.end()
           return
         }
+        if (rejectDesktopPluginMutation(response)) return
         if (!sameOrigin(request)) {
           sendJson(response, 403, { error: 'untrusted origin' })
           return
@@ -1482,7 +1542,7 @@ export function mountMarketRoutes(
           // survives a restart — a setting that forgets is a setting the
           // user has to make again every boot.
           marketState.channel = wanted
-          writeMarketState(activeProfileDir, marketState)
+          persistMarketState()
           // The cached listing was computed for the old channel, so the very
           // next check would answer for a setting that no longer applies.
           invalidateUpdates()
@@ -1503,6 +1563,7 @@ export function mountMarketRoutes(
           response.end()
           return
         }
+        if (rejectDesktopPluginMutation(response)) return
         // The same single door the restart route uses — and only it. Both
         // end the market's life in this process, so neither may be driven by
         // a remote or forwarded client. A separate `sameOrigin` call would
@@ -1761,6 +1822,7 @@ export function mountMarketRoutes(
           response.end()
           return
         }
+        if (rejectDesktopPluginMutation(response)) return
         if (!sameOrigin(request)) {
           sendJson(response, 403, { error: 'untrusted origin' })
           return
@@ -1818,7 +1880,7 @@ export function mountMarketRoutes(
               // rule so no group toggle ever targets a ghost member.
               disabled.delete(name)
               removeFromGroups({ groups, groupOrder }, name)
-              writeMarketState(activeProfileDir, { disabled, groups, groupOrder })
+              persistMarketState()
             }
             logEvent(ok || cancelled ? 'info' : 'error', 'uninstall',
               `${name} exit=${String(result.exitCode)}${cancelled ? ' CANCELLED' : ''}${ok ? ` live-removed=${String(hot)}` : cancelled ? '' : ` stderr=${result.stderr.slice(-300)}`}`)
@@ -1855,6 +1917,7 @@ export function mountMarketRoutes(
           response.end()
           return
         }
+        if (rejectDesktopPluginMutation(response)) return
         if (!sameOrigin(request)) {
           sendJson(response, 403, { error: 'untrusted origin' })
           return
@@ -1922,6 +1985,7 @@ export function mountMarketRoutes(
           response.end()
           return
         }
+        if (rejectDesktopPluginMutation(response)) return
         if (!sameOrigin(request)) {
           sendJson(response, 403, { error: 'untrusted origin' })
           return
@@ -2072,7 +2136,7 @@ export function mountMarketRoutes(
                 // (e.g. reinstall after an uninstall while this process kept
                 // running) and persist before the activation loop.
                 for (const name of added) disabled.delete(name)
-                writeMarketState(activeProfileDir, { disabled, groups, groupOrder })
+                persistMarketState()
                 // Theme installs auto-activate (and deactivate the previous
                 // theme) so the result is visible right after the refresh.
                 hot = true
diff --git a/src/themes.ts b/src/themes.ts
index 25596adb37af6116765a450f48c437d61b2d8097..98bc2e57b0095698c3f1346b4ca3f1cb695bb7f6 100644
--- a/src/themes.ts
+++ b/src/themes.ts
@@ -24,6 +24,12 @@ export interface ThemeHost {
   plugin(plugin: unknown, config: unknown): { await(): Promise<unknown>; dispose(): Promise<unknown> | void }
 }
 
+/** Desktop-owned durable skin state. Ordinary DSH keeps using state.json. */
+export interface ThemePersistence {
+  disabledNames(themeNames: Iterable<string>, entries: Iterable<LoaderEntry>): Set<string>
+  activateBundleTheme(name: string, themeNames: Iterable<string>, entries: Iterable<LoaderEntry>): void
+}
+
 /** Manages theme exclusivity for one profile. */
 export interface ThemeManager {
   installedThemeNames(): Promise<Set<string>>
@@ -41,6 +47,7 @@ export function createThemeManager(
   profile: string,
   disabledThemes: Set<string>,
   explicitDir?: string,
+  persistence?: ThemePersistence,
 ): ThemeManager {
   const activeProfileDir = profileDir(profile, explicitDir)
   /** Installed package names classified as themes by the registry's theme category. */
@@ -101,24 +108,37 @@ export function createThemeManager(
   /**
    * Make `name` the one active theme: deactivate every other installed theme
    * (market hot mounts unmount; bundle-layer entries live-disable) and bring
-   * it up. The choice persists in state.json and is replayed at boot.
+   * it up. Desktop persists into the shared home patch; ordinary DSH retains
+   * state.json for backward compatibility.
    */
   async function activateTheme(name: string): Promise<boolean> {
     const themes = await installedThemeNames()
+    // Desktop's home patch is the durable source of truth. Commit it before
+    // touching live entries so a rejected persistence request cannot leave a
+    // theme active only in memory.
+    if (persistence !== undefined) {
+      persistence.activateBundleTheme(name, themes, host.loader.entries())
+      disabledThemes.clear()
+      for (const disabled of persistence.disabledNames(themes, host.loader.entries())) disabledThemes.add(disabled)
+    }
     for (const other of themes) {
       if (other === name) continue
       if (listHotMounts().includes(other)) {
         await hotUnmount(other)
-        disabledThemes.add(other)
+        if (persistence === undefined) disabledThemes.add(other)
       } else if (await setEntryDisabled(other, true)) {
-        disabledThemes.add(other)
+        if (persistence === undefined) disabledThemes.add(other)
       }
     }
-    disabledThemes.delete(name)
-    writeDisabled(activeProfileDir, disabledThemes)
-    if (listHotMounts().includes(name)) return true
-    if (await setEntryDisabled(name, false)) return true
-    return (await hotMount(host, activeProfileDir, name)).ok
+    const activated = listHotMounts().includes(name)
+      || await setEntryDisabled(name, false)
+      || (await hotMount(host, activeProfileDir, name)).ok
+    if (!activated) return false
+    if (persistence === undefined) {
+      disabledThemes.delete(name)
+      writeDisabled(activeProfileDir, disabledThemes)
+    }
+    return true
   }
 
   return { installedThemeNames, setEntryDisabled, activateTheme }
diff --git a/src/verify.ts b/src/verify.ts
index 6f3bf751478ac0a273f6eb8222d1d6cd99bed331..c1c8f15e84d257005a3ea950d01c377fae3c9194 100644
--- a/src/verify.ts
+++ b/src/verify.ts
@@ -228,6 +228,17 @@ export function verifyActivation(
     }
   }
 
+  if (dsh.bundle !== undefined) {
+    return {
+      state: 'inert',
+      reasons: [
+        '已声明 dsh.bundle,但当前未进入 profile bundle 层;请通过宿主插件管理器启用 / dsh.bundle is declared but the package is not in the profile bundle layer; enable it through the host plugin manager',
+      ],
+      bundle: false,
+      hot: false,
+    }
+  }
+
   // Not a profile-layer plugin. Client-only packages never enter bundles
   // (the dsh CLI skips them), so the market shim-mounts them at boot —
   // they still work, but "installed" never means "bundle layer".
