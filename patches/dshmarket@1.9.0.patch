diff --git a/lib/hot.js b/lib/hot.js
index 5c337bddf4e0a351bb5db8f82aff4d483bf3f360..7b5af8fdc6ececd328efaca291ea8f4fb550081e 100644
--- a/lib/hot.js
+++ b/lib/hot.js
@@ -144,6 +144,10 @@ export function writeDisabledThemes(profileDir, disabled) {
     mkdirSync(join(profileDir, HOT_DIR), { recursive: true, mode: 0o700 });
     writeFileSync(stateFile(profileDir), JSON.stringify({ disabledSkins: [...disabled] }));
 }
+/** Remove the legacy market-only skin state after Desktop migrates it. */
+export function clearDisabledThemes(profileDir) {
+    rmSync(stateFile(profileDir), { force: true });
+}
 /** Package names currently live through a market hot mount (patch or shim). */
 export function listHotMounts() {
     return [...hotHandles.keys()];
diff --git a/lib/index.js b/lib/index.js
index 941803cd22bc46733d5c56f176dfb7f1b087bc05..1965375f015fce4501631734dec693f7946673a1 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -43,12 +43,17 @@ export function apply(ctx, config) {
             const current = desktopProfiles.current;
             const service = desktopCtx.desktopPnpm;
             const runtime = createDesktopPluginRuntime(service, current.dir);
+            const skinState = desktopCtx.get('desktopSkinState');
+            if (skinState === undefined) {
+                host.logger?.warn('[dsh-market] desktopSkinState unavailable; using legacy theme persistence');
+            }
             const resolved = {
                 profile: current.name,
                 profileDirectory: current.dir,
                 // Relaunching a raw Electron process would bypass Desktop's launcher
                 // lifecycle. The shell remains responsible for restart in this mode.
                 allowRestart: false,
+                skinState,
             };
             const desktopHost = desktopCtx;
             desktopHost.effect(() => {
diff --git a/lib/routes.js b/lib/routes.js
index 781ee9ab7359c324fac71c6ec4c0e89f291678bc..04e3440337dac8bee9c6b21f0704d6c6205b1217 100644
--- a/lib/routes.js
+++ b/lib/routes.js
@@ -10,7 +10,7 @@
 import { existsSync, readFileSync, writeFileSync } from 'node:fs';
 import { join } from 'node:path';
 import { loadRegistry } from './registry.js';
-import { cleanHotDir, hotMount, hotUnmount, listHotMounts, mountClientOnlyDeps, readDisabledThemes, } from './hot.js';
+import { cleanHotDir, clearDisabledThemes, hotMount, hotUnmount, listHotMounts, mountClientOnlyDeps, readDisabledThemes, writeDisabledThemes, } from './hot.js';
 import { exportLogs, logEvent } from './log.js';
 import { BOOT_ID, cancelActive, probePnpm, progress, provisionPnpm, runDshPlugin, } from './dsh-cli.js';
 import { profileDir, readInstalled, readInstalledVersion, readLockCommits, readManifestDeps, restoreManifestDeps, setAllowBuilds } from './profile.js';
@@ -58,8 +58,26 @@ export function mountMarketRoutes(host, config, commandRuntime) {
     // survive into a composition where the bundle layer already covers them.
     cleanHotDir(activeProfileDir);
     // The user's persisted theme choice; activateTheme mutates and writes it.
-    const disabledThemes = readDisabledThemes(activeProfileDir);
-    const themes = createThemeManager(host, config.profile, disabledThemes, activeProfileDir);
+    const legacyDisabledThemes = readDisabledThemes(activeProfileDir);
+    const disabledThemes = new Set(legacyDisabledThemes);
+    if (config.skinState !== undefined) {
+        const migrated = config.skinState.migrateLegacy(legacyDisabledThemes, host.loader.entries());
+        for (const name of migrated)
+            disabledThemes.delete(name);
+        if (disabledThemes.size === 0)
+            clearDisabledThemes(activeProfileDir);
+        else {
+            writeDisabledThemes(activeProfileDir, disabledThemes);
+            logEvent('warn', 'boot', `legacy theme disables pending migration: ${[...disabledThemes].join(', ')}`);
+        }
+    }
+    const themes = createThemeManager(host, config.profile, disabledThemes, activeProfileDir, config.skinState);
+    function persistLegacyFallback() {
+        if (disabledThemes.size === 0)
+            clearDisabledThemes(activeProfileDir);
+        else
+            writeDisabledThemes(activeProfileDir, disabledThemes);
+    }
     // Client-only packages (dsh.client without dsh.bundle) are invisible to the
     // bundle layer in every boot; the market shim-mounts them so their client
     // bundles are actually served.
@@ -74,14 +92,34 @@ export function mountMarketRoutes(host, config, commandRuntime) {
                 logEvent('info', 'boot', `theme kept off: ${name}`);
         }
     });
-    // Self-healing guard: dsh's own patch overlay can re-update entries during
-    // activation and wipe the runtime disabled flag — whenever a fiber comes up
-    // for a theme the user switched off, put it back down.
-    host.on?.('internal/plugin', (fiber) => {
-        const name = fiber.entry?.options?.name;
-        if (name !== undefined && disabledThemes.has(name))
+    // Ordinary DSH and unresolved legacy Desktop names retain the in-memory
+    // guard. A Desktop fallback removes itself from state.json as soon as the
+    // loader exposes enough information for durable migration.
+    if (disabledThemes.size > 0) {
+        host.on?.('internal/plugin', (fiber) => {
+            const name = fiber.entry?.options?.name;
+            if (name === undefined || !disabledThemes.has(name))
+                return;
             void themes.setEntryDisabled(name, true);
-    });
+            if (config.skinState !== undefined) {
+                const migrated = config.skinState.migrateLegacy([name], host.loader.entries());
+                if (migrated.has(name)) {
+                    disabledThemes.delete(name);
+                    persistLegacyFallback();
+                    logEvent('info', 'boot', `legacy theme disable migrated: ${name}`);
+                }
+            }
+        });
+    }
     let installing = false;
     let restarting = false;
+    function rejectDesktopPluginMutation(response) {
+        if (config.skinState === undefined)
+            return false;
+        sendJson(response, 409, {
+            code: 'desktop-plugin-manager-required',
+            error: '桌面版的插件安装、更新和卸载统一由“工具 -> 扩展坞”管理，以便启动失败时自动回滚。 / Desktop plugin changes must use Tools -> Extension Dock so failed startups can be rolled back.',
+        });
+        return true;
+    }
     /** Dependency diff vs. a pre-operation snapshot (cancel aftermath). */
@@ -457,5 +484,7 @@ export function mountMarketRoutes(host, config, commandRuntime) {
                     return;
                 }
+                if (rejectDesktopPluginMutation(response))
+                    return;
                 if (!sameOrigin(request)) {
                     sendJson(response, 403, { error: 'untrusted origin' });
                     return;
@@ -763,5 +792,7 @@ export function mountMarketRoutes(host, config, commandRuntime) {
                     return;
                 }
+                if (rejectDesktopPluginMutation(response))
+                    return;
                 if (!sameOrigin(request)) {
                     sendJson(response, 403, { error: 'untrusted origin' });
                     return;
@@ -842,5 +873,7 @@ export function mountMarketRoutes(host, config, commandRuntime) {
                     return;
                 }
+                if (rejectDesktopPluginMutation(response))
+                    return;
                 if (!sameOrigin(request)) {
                     sendJson(response, 403, { error: 'untrusted origin' });
                     return;
diff --git a/lib/themes.js b/lib/themes.js
index 274afae6425c6bb652cf66f529687bb6b18b79ab..1073cf92f02539e128ef89b1cb9a6512b1961140 100644
--- a/lib/themes.js
+++ b/lib/themes.js
@@ -14,7 +14,7 @@ import { repoOf } from './sources.js';
  * themes the user switched off — the caller owns reading it at boot and
  * replaying it; the manager mutates and persists it on switches.
  */
-export function createThemeManager(host, profile, disabledThemes, explicitDir) {
+export function createThemeManager(host, profile, disabledThemes, explicitDir, persistence) {
     const activeProfileDir = profileDir(profile, explicitDir);
     /** Installed package names classified as themes by the registry's theme category. */
     async function installedThemeNames() {
@@ -75,28 +75,43 @@ export function createThemeManager(host, profile, disabledThemes, explicitDir) {
     /**
      * Make `name` the one active theme: deactivate every other installed theme
      * (market hot mounts unmount; bundle-layer entries live-disable) and bring
-     * it up. The choice persists in state.json and is replayed at boot.
+     * it up. Desktop persists into the shared home patch; ordinary DSH retains
+     * state.json for backward compatibility.
      */
     async function activateTheme(name) {
         const themes = await installedThemeNames();
+        // Desktop's home patch is the durable source of truth. Commit it before
+        // touching live entries so a rejected persistence request cannot leave a
+        // theme active only in memory.
+        if (persistence !== undefined) {
+            persistence.activateBundleTheme(name, themes, host.loader.entries());
+            disabledThemes.clear();
+            for (const disabled of persistence.disabledNames(themes, host.loader.entries()))
+                disabledThemes.add(disabled);
+        }
         for (const other of themes) {
             if (other === name)
                 continue;
             if (listHotMounts().includes(other)) {
                 await hotUnmount(other);
-                disabledThemes.add(other);
+                if (persistence === undefined)
+                    disabledThemes.add(other);
             }
             else if (await setEntryDisabled(other, true)) {
-                disabledThemes.add(other);
+                if (persistence === undefined)
+                    disabledThemes.add(other);
             }
         }
-        disabledThemes.delete(name);
-        writeDisabledThemes(activeProfileDir, disabledThemes);
-        if (listHotMounts().includes(name))
-            return true;
-        if (await setEntryDisabled(name, false))
-            return true;
-        return (await hotMount(host, activeProfileDir, name)).ok;
+        const activated = listHotMounts().includes(name)
+            || await setEntryDisabled(name, false)
+            || (await hotMount(host, activeProfileDir, name)).ok;
+        if (!activated)
+            return false;
+        if (persistence === undefined) {
+            disabledThemes.delete(name);
+            writeDisabledThemes(activeProfileDir, disabledThemes);
+        }
+        return true;
     }
     return { installedThemeNames, setEntryDisabled, activateTheme };
 }
diff --git a/lib/types/hot.d.ts b/lib/types/hot.d.ts
index 6697805b317cb54ddfda2d3251b34ba5b1cbd97d..968f918aa86832754ef97340f45f984ce3e99f7e 100644
--- a/lib/types/hot.d.ts
+++ b/lib/types/hot.d.ts
@@ -42,6 +42,8 @@ export declare function cleanHotDir(profileDir: string): void;
 /** Themes the user switched away from; skipped by the boot re-mount. */
 export declare function readDisabledThemes(profileDir: string): Set<string>;
 export declare function writeDisabledThemes(profileDir: string, disabled: Set<string>): void;
+/** Remove the legacy market-only skin state after Desktop migrates it. */
+export declare function clearDisabledThemes(profileDir: string): void;
 /** Package names currently live through a market hot mount (patch or shim). */
 export declare function listHotMounts(): string[];
 /** Outcome of one hot-mount attempt; `reason` explains non-`ok` results. */
diff --git a/lib/types/routes.d.ts b/lib/types/routes.d.ts
index 235a387dbe668b5faaacac181c6f81c14474497b..f23f6234a82e3cda6fc75d671c1f52f0cc5d8469 100644
--- a/lib/types/routes.d.ts
+++ b/lib/types/routes.d.ts
@@ -9,7 +9,7 @@
  */
 import type { IncomingMessage, ServerResponse } from 'node:http';
 import { type PluginCommandRuntime } from './dsh-cli.ts';
-import { type LoaderEntry } from './themes.ts';
+import { type LoaderEntry, type ThemePersistence } from './themes.ts';
 export type { LoaderEntry } from './themes.ts';
 export type { UpdateStatus } from './updates.ts';
 export interface WebServerService {
@@ -47,6 +47,10 @@ export interface MarketConfig {
     profileDirectory?: string;
     /** Detached self-restart is unsafe under systemd/launchd/pm2; operators can disable it (#14). */
     allowRestart?: boolean;
+    /** Desktop's shared home-patch channel; absent in ordinary DSH. */
+    skinState?: ThemePersistence & {
+        migrateLegacy(disabledNames: Iterable<string>, entries: Iterable<LoaderEntry>): Set<string>;
+    };
 }
 /**
  * Register the market's HTTP routes.
diff --git a/lib/types/themes.d.ts b/lib/types/themes.d.ts
index 5b152d786883c0967a4ac976a6964c0ce02f81a7..fcffa8c673edd41b64a7d125049700f7bc564dca 100644
--- a/lib/types/themes.d.ts
+++ b/lib/types/themes.d.ts
@@ -26,6 +26,11 @@ export interface ThemeHost {
         dispose(): Promise<unknown> | void;
     };
 }
+/** Desktop-owned durable skin state. Ordinary DSH keeps using state.json. */
+export interface ThemePersistence {
+    disabledNames(themeNames: Iterable<string>, entries: Iterable<LoaderEntry>): Set<string>;
+    activateBundleTheme(name: string, themeNames: Iterable<string>, entries: Iterable<LoaderEntry>): void;
+}
 /** Manages theme exclusivity for one profile. */
 export interface ThemeManager {
     installedThemeNames(): Promise<Set<string>>;
@@ -37,4 +42,4 @@ export interface ThemeManager {
  * themes the user switched off — the caller owns reading it at boot and
  * replaying it; the manager mutates and persists it on switches.
  */
-export declare function createThemeManager(host: ThemeHost, profile: string, disabledThemes: Set<string>, explicitDir?: string): ThemeManager;
+export declare function createThemeManager(host: ThemeHost, profile: string, disabledThemes: Set<string>, explicitDir?: string, persistence?: ThemePersistence): ThemeManager;
diff --git a/src/hot.ts b/src/hot.ts
index 407cf0f0fba2e67890f3aa0d15e4c378c5345942..14bfa5eaa6aa9dd48a03439be85d4b24d789ec97 100644
--- a/src/hot.ts
+++ b/src/hot.ts
@@ -158,6 +158,11 @@ export function writeDisabledThemes(profileDir: string, disabled: Set<string>):
   writeFileSync(stateFile(profileDir), JSON.stringify({ disabledSkins: [...disabled] }))
 }
 
+/** Remove the legacy market-only skin state after Desktop migrates it. */
+export function clearDisabledThemes(profileDir: string): void {
+  rmSync(stateFile(profileDir), { force: true })
+}
+
 /** Package names currently live through a market hot mount (patch or shim). */
 export function listHotMounts(): string[] {
   return [...hotHandles.keys()]
@@ -335,4 +340,3 @@ export function patchLayerManages(controls: { ids: Set<string>; names: Set<strin
   const rowId = name.replace(/^@/, '').replace(/[^a-z0-9-]/gi, '-').toLowerCase()
   return controls.ids.has(rowId) || controls.names.has(name)
 }
-
diff --git a/src/index.ts b/src/index.ts
index ca084f711bfccf37d8261f1ce653e2553ea75dce..2a9b2a2e8f20b64e425d13f4b410fd76d266a8bc 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -66,12 +66,17 @@ export function apply(ctx: Context, config?: Config): void {
       const current = desktopProfiles.current
       const service = (desktopCtx as unknown as { desktopPnpm: DesktopPnpmLike }).desktopPnpm
       const runtime = createDesktopPluginRuntime(service, current.dir)
+      const skinState = desktopCtx.get('desktopSkinState') as MarketConfig['skinState']
+      if (skinState === undefined) {
+        host.logger?.warn('[dsh-market] desktopSkinState unavailable; using legacy theme persistence')
+      }
       const resolved: MarketConfig = {
         profile: current.name,
         profileDirectory: current.dir,
         // Relaunching a raw Electron process would bypass Desktop's launcher
         // lifecycle. The shell remains responsible for restart in this mode.
         allowRestart: false,
+        skinState,
       }
       const desktopHost = desktopCtx as unknown as MarketEffectHost
       desktopHost.effect(() => {
diff --git a/src/routes.ts b/src/routes.ts
index 091a35407be7d4e80e5ef4749cb0ebf884a92c4d..e8c02f3fe244c55ca170d0c44bb4295691f6bf44 100644
--- a/src/routes.ts
+++ b/src/routes.ts
@@ -14,7 +14,7 @@ import type { IncomingMessage, ServerResponse } from 'node:http'
 import { loadRegistry } from './registry.ts'
 import {
   cleanHotDir, hotMount, hotUnmount, listHotMounts,
-  mountClientOnlyDeps, readDisabledThemes,
+  clearDisabledThemes, mountClientOnlyDeps, readDisabledThemes, writeDisabledThemes,
 } from './hot.ts'
 import { exportLogs, logEvent } from './log.ts'
 import {
@@ -25,7 +25,7 @@ import { profileDir, readInstalled, readInstalledVersion, readLockCommits, readM
 import { findInstalledAlias, gitAllowBuildsKey, installTargetFor } from './sources.ts'
 import { isStaleUpdate, parseIgnoredBuilds, parsePrepareNotAllowed, RELEASE_AGE_OVERRIDE, retargetCollections, validateAddedPlugins, withHoistRecovery } from './install.ts'
 import { checkUpdates, fetchNpmLatest, invalidateUpdates, isUpgrade, latestPublishedRecently } from './updates.ts'
-import { createThemeManager, type LoaderEntry } from './themes.ts'
+import { createThemeManager, type LoaderEntry, type ThemePersistence } from './themes.ts'
 import { readJsonBody, sameOrigin, sendJson } from './http.ts'
 import { restartAllowed, scheduleRestart, trustedRestartRequest, trustedDownloadRequest } from './restart.ts'
 import { verifyActivation } from './verify.ts'
@@ -59,6 +59,10 @@ export interface MarketConfig {
   profileDirectory?: string
   /** Detached self-restart is unsafe under systemd/launchd/pm2; operators can disable it (#14). */
   allowRestart?: boolean
+  /** Desktop's shared home-patch channel; absent in ordinary DSH. */
+  skinState?: ThemePersistence & {
+    migrateLegacy(disabledNames: Iterable<string>, entries: Iterable<LoaderEntry>): Set<string>
+  }
 }
 
 const PROFILE_RE = /^[A-Za-z0-9_-]+$/
@@ -101,8 +105,23 @@ export function mountMarketRoutes(
   // survive into a composition where the bundle layer already covers them.
   cleanHotDir(activeProfileDir)
   // The user's persisted theme choice; activateTheme mutates and writes it.
-  const disabledThemes = readDisabledThemes(activeProfileDir)
-  const themes = createThemeManager(host, config.profile, disabledThemes, activeProfileDir)
+  const legacyDisabledThemes = readDisabledThemes(activeProfileDir)
+  const disabledThemes = new Set(legacyDisabledThemes)
+  if (config.skinState !== undefined) {
+    const migrated = config.skinState.migrateLegacy(legacyDisabledThemes, host.loader.entries())
+    for (const name of migrated) disabledThemes.delete(name)
+    if (disabledThemes.size === 0) clearDisabledThemes(activeProfileDir)
+    else {
+      writeDisabledThemes(activeProfileDir, disabledThemes)
+      logEvent('warn', 'boot', `legacy theme disables pending migration: ${[...disabledThemes].join(', ')}`)
+    }
+  }
+  const themes = createThemeManager(host, config.profile, disabledThemes, activeProfileDir, config.skinState)
+
+  function persistLegacyFallback(): void {
+    if (disabledThemes.size === 0) clearDisabledThemes(activeProfileDir)
+    else writeDisabledThemes(activeProfileDir, disabledThemes)
+  }
 
   // Client-only packages (dsh.client without dsh.bundle) are invisible to the
   // bundle layer in every boot; the market shim-mounts them so their client
@@ -117,13 +136,33 @@ export function mountMarketRoutes(
     }
   })
 
-  // Self-healing guard: dsh's own patch overlay can re-update entries during
-  // activation and wipe the runtime disabled flag — whenever a fiber comes up
-  // for a theme the user switched off, put it back down.
-  host.on?.('internal/plugin', (fiber) => {
-    const name = fiber.entry?.options?.name
-    if (name !== undefined && disabledThemes.has(name)) void themes.setEntryDisabled(name, true)
-  })
+  // Ordinary DSH and unresolved legacy Desktop names retain the in-memory
+  // guard. A Desktop fallback removes itself from state.json as soon as the
+  // loader exposes enough information for durable migration.
+  if (disabledThemes.size > 0) {
+    host.on?.('internal/plugin', (fiber) => {
+      const name = fiber.entry?.options?.name
+      if (name === undefined || !disabledThemes.has(name)) return
+      void themes.setEntryDisabled(name, true)
+      if (config.skinState !== undefined) {
+        const migrated = config.skinState.migrateLegacy([name], host.loader.entries())
+        if (migrated.has(name)) {
+          disabledThemes.delete(name)
+          persistLegacyFallback()
+          logEvent('info', 'boot', `legacy theme disable migrated: ${name}`)
+        }
+      }
+    })
+  }
   let installing = false
   let restarting = false
+  function rejectDesktopPluginMutation(response: ServerResponse): boolean {
+    if (config.skinState === undefined) return false
+    sendJson(response, 409, {
+      code: 'desktop-plugin-manager-required',
+      error: '桌面版的插件安装、更新和卸载统一由“工具 -> 扩展坞”管理，以便启动失败时自动回滚。 / Desktop plugin changes must use Tools -> Extension Dock so failed startups can be rolled back.',
+    })
+    return true
+  }
+
 
@@ -493,6 +521,7 @@ export function mountMarketRoutes(
           response.end()
           return
         }
+        if (rejectDesktopPluginMutation(response)) return
         if (!sameOrigin(request)) {
           sendJson(response, 403, { error: 'untrusted origin' })
           return
@@ -796,6 +828,7 @@ export function mountMarketRoutes(
           response.end()
           return
         }
+        if (rejectDesktopPluginMutation(response)) return
         if (!sameOrigin(request)) {
           sendJson(response, 403, { error: 'untrusted origin' })
           return
@@ -874,6 +910,7 @@ export function mountMarketRoutes(
           response.end()
           return
         }
+        if (rejectDesktopPluginMutation(response)) return
         if (!sameOrigin(request)) {
           sendJson(response, 403, { error: 'untrusted origin' })
           return
diff --git a/src/themes.ts b/src/themes.ts
index 84c545451f7c31eaea7eefc78d8a8e61658dd105..3fa3ecba52c7d1c6bdb82c00f16b476d44ffe031 100644
--- a/src/themes.ts
+++ b/src/themes.ts
@@ -24,6 +24,12 @@ export interface ThemeHost {
   plugin(plugin: unknown, config: unknown): { await(): Promise<unknown>; dispose(): Promise<unknown> | void }
 }
 
+/** Desktop-owned durable skin state. Ordinary DSH keeps using state.json. */
+export interface ThemePersistence {
+  disabledNames(themeNames: Iterable<string>, entries: Iterable<LoaderEntry>): Set<string>
+  activateBundleTheme(name: string, themeNames: Iterable<string>, entries: Iterable<LoaderEntry>): void
+}
+
 /** Manages theme exclusivity for one profile. */
 export interface ThemeManager {
   installedThemeNames(): Promise<Set<string>>
@@ -41,6 +47,7 @@ export function createThemeManager(
   profile: string,
   disabledThemes: Set<string>,
   explicitDir?: string,
+  persistence?: ThemePersistence,
 ): ThemeManager {
   const activeProfileDir = profileDir(profile, explicitDir)
   /** Installed package names classified as themes by the registry's theme category. */
@@ -101,24 +108,37 @@ export function createThemeManager(
   /**
    * Make `name` the one active theme: deactivate every other installed theme
    * (market hot mounts unmount; bundle-layer entries live-disable) and bring
-   * it up. The choice persists in state.json and is replayed at boot.
+   * it up. Desktop persists into the shared home patch; ordinary DSH retains
+   * state.json for backward compatibility.
    */
   async function activateTheme(name: string): Promise<boolean> {
     const themes = await installedThemeNames()
+    // Desktop's home patch is the durable source of truth. Commit it before
+    // touching live entries so a rejected persistence request cannot leave a
+    // theme active only in memory.
+    if (persistence !== undefined) {
+      persistence.activateBundleTheme(name, themes, host.loader.entries())
+      disabledThemes.clear()
+      for (const disabled of persistence.disabledNames(themes, host.loader.entries())) disabledThemes.add(disabled)
+    }
     for (const other of themes) {
       if (other === name) continue
       if (listHotMounts().includes(other)) {
         await hotUnmount(other)
-        disabledThemes.add(other)
+        if (persistence === undefined) disabledThemes.add(other)
       } else if (await setEntryDisabled(other, true)) {
-        disabledThemes.add(other)
+        if (persistence === undefined) disabledThemes.add(other)
       }
     }
-    disabledThemes.delete(name)
-    writeDisabledThemes(activeProfileDir, disabledThemes)
-    if (listHotMounts().includes(name)) return true
-    if (await setEntryDisabled(name, false)) return true
-    return (await hotMount(host, activeProfileDir, name)).ok
+    const activated = listHotMounts().includes(name)
+      || await setEntryDisabled(name, false)
+      || (await hotMount(host, activeProfileDir, name)).ok
+    if (!activated) return false
+    if (persistence === undefined) {
+      disabledThemes.delete(name)
+      writeDisabledThemes(activeProfileDir, disabledThemes)
+    }
+    return true
   }
 
   return { installedThemeNames, setEntryDisabled, activateTheme }
